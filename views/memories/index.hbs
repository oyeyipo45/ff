<h1>memories</h1>
<div class="row">
     {{!-- this user is gotten from the global variable, and is  called from here,this is the user outside the loop, logged in user (the ../ place
     before the user takes us one level to outside the loop to access the logged in user no story user)--}}
    {{#each memories}}
    <div class="col s12 m4">
        <div class="card">
            <div class="card-image">
                {{!-- the first user is the user within the loop, the user the owner of the story --}}
                {{{editIcon user ../user _id}}}
            </div>
            <div class="card-content center-align">
                <h5>{{title}}</h5>
                <p> {{stripTags (truncate body 150)}}</p>

                <br>
                <div class="chip">
                    <img src="{{user.image}}" alt="">
                    <a href="/memories/user/{{user._id}}">{{user.displayName}} </a>
                </div>
            </div>


            <div class="card-action center-align">
                <a href="/memories/{{_id}}" class="btn grey">Read More</a>
            </div>
        </div>
    </div>
    {{else}}

    <p>No Memory to display</p>
    {{/each}}
</div>